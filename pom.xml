<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.eventhub</groupId>
    <artifactId>eventhub-selenium-bdd</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>EventHub Selenium Java BDD Framework</name>
    <description>Enterprise-grade Selenium Java, Cucumber BDD, TestNG, Rest Assured automation framework for EventHub.</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.release>17</maven.compiler.release>
        <selenium.version>4.23.1</selenium.version>
        <webdrivermanager.version>5.9.2</webdrivermanager.version>
        <cucumber.version>7.18.1</cucumber.version>
        <testng.version>7.10.2</testng.version>
        <restassured.version>5.5.0</restassured.version>
        <jackson.version>2.17.2</jackson.version>
        <log4j2.version>2.23.1</log4j2.version>
        <allure.version>2.29.0</allure.version>
        <axe.selenium.version>4.10.2</axe.selenium.version>
        <aspectj.version>1.9.22.1</aspectj.version>
        <surefire.version>3.3.1</surefire.version>
        <maven.enforcer.version>3.5.0</maven.enforcer.version>
        <spotless.version>2.43.0</spotless.version>
        <dependency.check.version>10.0.4</dependency.check.version>
        <maven.clean.version>3.2.0</maven.clean.version>
        <maven.jar.version>3.5.0</maven.jar.version>
        <maven.install.version>3.1.4</maven.install.version>
        <maven.site.version>3.12.1</maven.site.version>
        <maven.deploy.version>3.1.4</maven.deploy.version>
        <browser>chrome</browser>
        <headless>false</headless>
        <execution.target>local</execution.target>
        <selenium.remote.url>http://localhost:4444/wd/hub</selenium.remote.url>
        <environment>qa</environment>
        <suite.name>local</suite.name>
        <parallel>none</parallel>
        <thread.count>2</thread.count>
        <retry.count>0</retry.count>
        <retry.tags>@retryable</retry.tags>
        <retry.max.allowed>0</retry.max.allowed>
        <preflight.enabled>true</preflight.enabled>
        <cleanup.before.run>false</cleanup.before.run>
        <accessibility.threshold.enabled>false</accessibility.threshold.enabled>
        <accessibility.max.violations>0</accessibility.max.violations>
        <visual.baseline.enabled>false</visual.baseline.enabled>
        <visual.baseline.update>false</visual.baseline.update>
        <visual.baseline.dir>src/test/resources/visual-baselines</visual.baseline.dir>
        <visual.diff.max.pixels>0</visual.diff.max.pixels>
        <suite.xml.file>target/test-classes/suites/testng.xml</suite.xml.file>
        <cucumber.filter.tags>not @wip</cucumber.filter.tags>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.cucumber</groupId>
                <artifactId>cucumber-bom</artifactId>
                <version>${cucumber.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>${selenium.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.bonigarcia</groupId>
            <artifactId>webdrivermanager</artifactId>
            <version>${webdrivermanager.version}</version>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>${cucumber.version}</version>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-testng</artifactId>
            <version>${cucumber.version}</version>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-picocontainer</artifactId>
            <version>${cucumber.version}</version>
        </dependency>
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>${testng.version}</version>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <version>${restassured.version}</version>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>${restassured.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.deque.html.axe-core</groupId>
            <artifactId>selenium</artifactId>
            <version>${axe.selenium.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>${log4j2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
            <version>${log4j2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j2-impl</artifactId>
            <version>${log4j2.version}</version>
        </dependency>
        <dependency>
            <groupId>com.aventstack</groupId>
            <artifactId>extentreports</artifactId>
            <version>5.1.2</version>
        </dependency>
        <dependency>
            <groupId>tech.grasshopper</groupId>
            <artifactId>extentreports-cucumber7-adapter</artifactId>
            <version>1.14.0</version>
        </dependency>
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-cucumber7-jvm</artifactId>
            <version>${allure.version}</version>
        </dependency>
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-testng</artifactId>
            <version>${allure.version}</version>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.26.3</version>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven.clean.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven.jar.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>${maven.install.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>${maven.site.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven.deploy.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <propertiesEncoding>${project.build.sourceEncoding}</propertiesEncoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
                <configuration>
                    <suiteXmlFiles>
                        <suiteXmlFile>${suite.xml.file}</suiteXmlFile>
                    </suiteXmlFiles>
                    <argLine>
                        -javaagent:${settings.localRepository}/org/aspectj/aspectjweaver/${aspectj.version}/aspectjweaver-${aspectj.version}.jar
                        -Djava.util.logging.config.file=${project.basedir}/src/test/resources/logging.properties
                    </argLine>
                    <systemPropertyVariables>
                        <browser>${browser}</browser>
                        <headless>${headless}</headless>
                        <execution.target>${execution.target}</execution.target>
                        <selenium.remote.url>${selenium.remote.url}</selenium.remote.url>
                        <environment>${environment}</environment>
                        <suite.name>${suite.name}</suite.name>
                        <parallel>${parallel}</parallel>
                        <thread.count>${thread.count}</thread.count>
                        <retry.count>${retry.count}</retry.count>
                        <retry.tags>${retry.tags}</retry.tags>
                        <retry.max.allowed>${retry.max.allowed}</retry.max.allowed>
                        <preflight.enabled>${preflight.enabled}</preflight.enabled>
                        <cleanup.before.run>${cleanup.before.run}</cleanup.before.run>
                        <accessibility.threshold.enabled>${accessibility.threshold.enabled}</accessibility.threshold.enabled>
                        <accessibility.max.violations>${accessibility.max.violations}</accessibility.max.violations>
                        <visual.baseline.enabled>${visual.baseline.enabled}</visual.baseline.enabled>
                        <visual.baseline.update>${visual.baseline.update}</visual.baseline.update>
                        <visual.baseline.dir>${visual.baseline.dir}</visual.baseline.dir>
                        <visual.diff.max.pixels>${visual.diff.max.pixels}</visual.diff.max.pixels>
                        <cucumber.filter.tags>${cucumber.filter.tags}</cucumber.filter.tags>
                    </systemPropertyVariables>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.aspectj</groupId>
                        <artifactId>aspectjweaver</artifactId>
                        <version>${aspectj.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>io.qameta.allure</groupId>
                <artifactId>allure-maven</artifactId>
                <version>2.12.0</version>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>quality</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-enforcer-plugin</artifactId>
                        <version>${maven.enforcer.version}</version>
                        <executions>
                            <execution>
                                <id>enforce-build-baseline</id>
                                <phase>validate</phase>
                                <goals>
                                    <goal>enforce</goal>
                                </goals>
                                <configuration>
                                    <rules>
                                        <requireJavaVersion>
                                            <version>[17,)</version>
                                        </requireJavaVersion>
                                        <requireMavenVersion>
                                            <version>[3.8.6,)</version>
                                        </requireMavenVersion>
                                        <requirePluginVersions/>
                                    </rules>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>com.diffplug.spotless</groupId>
                        <artifactId>spotless-maven-plugin</artifactId>
                        <version>${spotless.version}</version>
                        <executions>
                            <execution>
                                <id>spotless-check</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <java>
                                <removeUnusedImports/>
                                <trimTrailingWhitespace/>
                                <endWithNewline/>
                            </java>
                            <formats>
                                <format>
                                    <includes>
                                        <include>**/*.md</include>
                                        <include>**/*.yml</include>
                                        <include>**/*.yaml</include>
                                        <include>**/*.json</include>
                                        <include>**/*.properties</include>
                                    </includes>
                                    <excludes>
                                        <exclude>target/**</exclude>
                                        <exclude>allure-results/**</exclude>
                                        <exclude>test-output/**</exclude>
                                    </excludes>
                                    <trimTrailingWhitespace/>
                                    <endWithNewline/>
                                </format>
                            </formats>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>security</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>${dependency.check.version}</version>
                        <configuration>
                            <failBuildOnCVSS>8</failBuildOnCVSS>
                            <formats>
                                <format>HTML</format>
                                <format>JSON</format>
                            </formats>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>accessibility-strict</id>
            <properties>
                <headless>true</headless>
                <cucumber.filter.tags>@accessibility</cucumber.filter.tags>
                <accessibility.threshold.enabled>true</accessibility.threshold.enabled>
                <accessibility.max.violations>3</accessibility.max.violations>
            </properties>
        </profile>
        <profile>
            <id>visual-baseline</id>
            <properties>
                <headless>true</headless>
                <cucumber.filter.tags>@visual</cucumber.filter.tags>
                <visual.baseline.enabled>true</visual.baseline.enabled>
            </properties>
        </profile>
    </profiles>
</project>
